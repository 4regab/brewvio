# Build output
bin/
obj/
[Dd]ebug/
[Rr]elease/
.aws-sam/
.agents*
# IDE
.vs/
.vscode/
.idea/
*.user
tmp*
# Local SQLite/Postgres data & secrets
*.db
*.db-shm
*.db-wal
# appsettings.*.local.json pattern kept as a safety net in case the file is recreated
appsettings.*.local.json

# Environment / secrets
.env
.env.local
.env.*.local

# Local run/test artifacts
api-run.log

# OS
.DS_Store
Thumbs.db

# Infrastructure templates (deployed out-of-band, kept local only)
# infra/ — removed from gitignore; IAM templates contain no secrets and should be version-controlled
